import React, { useRef } from 'react'

const Add = ({fetchprojects}) => {

    const titleref = useRef(null)
    const descref = useRef(null)
    const linkref = useRef(null)
    const coverref = useRef(null)


    const handle clic


  return (
    <>


      
    </>
  )
}

export default Add
